[pytest]
markers =
    unit: fast isolated behavior tests
    semantic: semantic parser, normalizer, rule, and accounting invariant tests
    integration: multi-module workflow or persistence boundary tests
    data_heavy: tests that require large local datasets, subprocess pools, downloads, or external services
addopts = --strict-markers
